# Design reference (v0)

Materials from the original build sprint, kept for provenance — not served or linked by the site.

- **`code.html`** — the initial Google Stitch export (Tailwind CDN) that Phase 2 of the build refactored into the current semantic HTML/CSS at the repo root. See [`AGENTS.md`](../../AGENTS.md) for the full process log.
- **`screen.png`** — the original Stitch design mock the export was based on.

Useful as a before/after: compare this markup with the current `index.html` (custom properties, no framework, ~777 lines of hand-written CSS).
